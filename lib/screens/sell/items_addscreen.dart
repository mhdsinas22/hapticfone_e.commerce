import 'package:flutter/material.dart';

class ItemsAddscreen extends StatelessWidget {
  const ItemsAddscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("ADD SCREEN")));
  }
}
